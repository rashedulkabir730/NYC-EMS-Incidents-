# Core data pipeline dependencies
requests==2.31.0
duckdb==0.10.0
sodapy==2.1.1
ratelimit==2.2.1

# dbt for DuckDB
dbt-duckdb==1.7.2

# Dashboard
streamlit>=1.42.0
altair>=5.0.0

# Optional but recommended
python-dotenv==1.0.1
pandas==2.2.0
pyarrow==15.0.0
# RAG / chatbot
chromadb
anthropic
pyyaml
