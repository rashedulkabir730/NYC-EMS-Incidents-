# Core data pipeline dependencies
requests==2.31.0
duckdb>=1.1.0
sodapy==2.1.1
ratelimit==2.2.1

# dbt for DuckDB
dbt-duckdb==1.7.2

# Dashboard
streamlit>=1.42.0
altair>=5.0.0

# Optional but recommended
python-dotenv==1.0.1
pandas==2.2.0
pyarrow>=16.0.0
# dlt ingestion
dlt[duckdb]>=1.4.0

# RAG / chatbot
chromadb
anthropic
pyyaml
